/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api.deezer.com', 'e-cdns-images.dzcdn.net', 'cdn-images.dzcdn.net'],
    },
};

export default nextConfig;
